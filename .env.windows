# Database Configuration - MongoDB Atlas (Cloud)
# Get your free MongoDB Atlas cluster at: https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/popcat?retryWrites=true&w=majority&appName=Cluster0

# Backend Server Configuration
BACKEND_PORT=3001

# Frontend Server Configuration
FRONTEND_PORT=3000

# Frontend API URL (must match BACKEND_PORT)
NEXT_PUBLIC_API_URL=http://localhost:3001